import { TodoApp } from '../../src/reducers';

const main = () => {
    test('probemos el todo reducer app (Pero es el primero!)',() => {
        
    })
};

describe('todo reducer app (El primero!!!)',main);